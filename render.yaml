services:
  - type: web
    name: safety-monitor-server
    env: node
    buildCommand: cd server && npm install
    startCommand: cd server && node ./server.js
    envVars:
      - key: PORT
        value: 3000 